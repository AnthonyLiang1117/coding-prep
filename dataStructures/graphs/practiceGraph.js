/*

Implementing a graph class
- graphs are a bunch of vertices (nodes) connected with edges (connections)
- we will be implementing a undirected unweighted graph and storing it in an adjacency list
- has an adjacencyList property that will be initialized as an js object

addVertex
addEdge
removeEdge
removeVertex

*/

class Graph {
  constructor() {
    this.adjacencyList = {};
  }
}
